# Dependencies for server.py (local FastAPI 3D scan server).
# Install with: pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
modal>=0.62

# Required for POST /api/scan/save-video (GCS upload):
google-cloud-storage>=2.16
google-auth>=2.29

# Patient Aura asset pipeline (background removal, skin maps):
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0

# Optional: higher-quality background removal for /clinic-scan/ (/remove-bg endpoint):
# rembg>=2.0

# Load .env.scan-server for local scan-server credentials:
python-dotenv>=1.0
